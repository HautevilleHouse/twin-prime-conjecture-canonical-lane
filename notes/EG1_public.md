# EG1 Public Note

Gate: `TP_G1`
Constant: `kappa_sieve`

This note isolates the coercive response package. The projected response operator is

`E_tau = Pi_resp L_tau Pi_resp`.

The gate closes once the admissible tube carries a uniform positive floor on the projected response sector. The extracted theorem-level constant is `kappa_sieve > 0`, obtained from the raw formula

`c_sieve_raw * level_density_raw - e_sieve_raw`.

In mainstream terms, this is the local positive-response statement that blocks degeneration of the primary transport mechanism.
