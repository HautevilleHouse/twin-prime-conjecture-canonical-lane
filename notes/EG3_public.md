# EG3 Public Note

Gate: `TP_G3`
Constant: `kappa_compact`

This note isolates compactness and no-collapse spacing. Near-failure normalized families live in the compactness carrier `K_tau`; the admissible restart windows are separated by a positive continuation interval. The extracted theorem-level constant is `kappa_compact > 0`, obtained from

`1.0 / (1.0 + delta_comp_sup_raw)`.

The gate closes once bad sequences admit convergent subsequences and restart times do not accumulate.
